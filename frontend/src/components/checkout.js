import React from 'react'

function Checkout({totalPrice}) {
    return (
        <div>
            checkout component
            total price: {totalPrice}
        </div>
    )
}

export default Checkout
